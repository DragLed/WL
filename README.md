# 📌 WL — Vue 3 + Vite приложение

WL — это веб-приложение, разработанное на **Vue 3** с использованием **Vite**. Проект представляет собой клиентскую часть (frontend) и предназначен для разработки одностраничного приложения (SPA) с современной структурой и быстрой сборкой.

Репозиторий содержит только файлы проекта и их фактическую функциональность без добавления сторонней логики или предположений.

---

## 🧠 Оглавление

* Описание проекта
* Используемые технологии
* Структура проекта
* Установка и запуск
* npm-скрипты
* Принцип работы проекта
* Сборка проекта

---

## 📌 Описание проекта

WL — это базовое Vue-приложение, созданное через Vite. Проект предназначен для работы в браузере и использует стандартный подход Vue 3 с компонентной архитектурой. Основная цель проекта — служить основой для разработки и тестирования клиентского интерфейса.

---

## 🛠 Используемые технологии

В проекте используются следующие технологии и инструменты:

* **Vue 3** — фреймворк для построения пользовательского интерфейса
* **Vite** — инструмент сборки и dev-сервер
* **JavaScript** — основной язык логики
* **HTML** — разметка
* **CSS** — стилизация
* **Node.js / npm** — управление зависимостями

---

## 📂 Структура проекта

```
WL/
├─ .vscode/                # Настройки редактора
├─ public/                 # Статические файлы
├─ src/                    # Исходный код приложения
│  ├─ assets/              # Ресурсы (изображения, стили)
│  ├─ components/          # Vue-компоненты
│  ├─ App.vue              # Корневой компонент приложения
│  └─ main.js              # Точка входа приложения
├─ index.html              # Основной HTML-файл
├─ package.json            # Зависимости и npm-скрипты
├─ package-lock.json       # Зафиксированные версии зависимостей
├─ vite.config.js          # Конфигурация Vite
├─ jsconfig.json           # Настройки JavaScript и путей
└─ .gitignore              # Файлы, игнорируемые Git
```

---

## 🚀 Установка и запуск

### 1. Клонирование репозитория

```
git clone https://github.com/DragLed/WL.git
cd WL
```

### 2. Установка зависимостей

```
npm install
```

### 3. Запуск в режиме разработки

```
npm run dev
```

После запуска проект будет доступен в браузере через локальный dev-сервер Vite.

---

## 🏃 npm-скрипты

| Команда           | Назначение                         |
| ----------------- | ---------------------------------- |
| `npm run dev`     | Запуск проекта в режиме разработки |
| `npm run build`   | Сборка проекта для продакшена      |
| `npm run preview` | Предпросмотр собранного проекта    |

---

## ⚙️ Принцип работы проекта

* Приложение инициализируется в файле `main.js`
* Корневым компонентом является `App.vue`
* Логика интерфейса разбита на компоненты в папке `components`
* Статические файлы подключаются через `public`
* Конфигурация сборки и алиасов задаётся в `vite.config.js` и `jsconfig.json`

Проект использует стандартный жизненный цикл Vue 3 без дополнительных библиотек.

---

## 📦 Сборка проекта

Для создания оптимизированной версии приложения выполните:

```
npm run build
```

В результате будет создана папка `dist`, содержащая готовые файлы для размещения на сервере или хостинге.

---

## 📄 Примечание

Проект является клиентским и не содержит серверной части. Вся логика выполняется в браузере.
